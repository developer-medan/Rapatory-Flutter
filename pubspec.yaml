name: rapatory_flutter
description: Attendance with fingerprint and GPS
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2

  # A powerful HTTP client for Dart, which supports Interceptors,
  # Global configuration, FormData, Request Cancellation,
  # File downloading, Timeout etc.
  dio: 2.1.13

  # Automatically generate code for converting to and from JSON by
  # annotation Dart classes.
  json_serializable: ^3.1.0

  # Vector design and runtime animation for Flutter.
  flare_flutter: ^1.5.4

  # ReactiveX for Dart
  rxdart: ^0.22.0

  # Flutter plugin for reading and writing simple key-value pairs.
  shared_preferences: ^0.5.3+4

  # Flutter plugin for getting commonly used locations on the Android & iOS.
  path_provider: ^1.1.0

  # Flutter plugin for querying information about the application package, such as
  # CFBundleVersion on iOS or versionCode on Android.
  package_info: ^0.4.0+5

  # Flutter plugin for integrating Google Maps in iOS and Android
  google_maps_flutter: ^0.5.19+2

  # Flutter plugin to easily handle realtime location in iOS and Android.
  # Provides settings for optimizing performance or battery.
  location: ^2.3.5

  # Flutter plugin for Android and iOS device authentication sensors such
  # as Fingerprint Reader and Touch ID.
  local_auth: ^0.5.2+4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Tools to write binaries that run builders.
  build_runner: ^1.6.1

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/material_fingerprint.flr
    - assets/images/img_sample_photo_profile.jpg
    - assets/images/img_placeholder.jpg
    - assets/images/img_illustration_clock_in_out.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
